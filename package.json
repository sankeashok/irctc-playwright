{
  "scripts": {
    "test": "call run-tests.bat --headed",
    "test:headless": "call run-tests.bat",
    "report": "npx allure generate ./allure-results -o allure-report && start \"\" \"%cd%\\allure-report\\index.html\""
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "allure-playwright": "^3.6.0",
    "dotenv": "^17.3.1",
    "playwright": "^1.58.2"
  }
}
